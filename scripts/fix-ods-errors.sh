#!/bin/bash
# Script pour corriger les erreurs ODS dans tous les fichiers

# Correction des imports CSS dans main.tsx (déjà fait manuellement)

# Correction de Text: size -> preset
# Correction de Button: variant="primary" -> variant="default" color="primary"
# Correction de Badge: variant -> color
# Suppression des imports inutilisés

echo "Correction des erreurs ODS en cours..."

